# 配置文件参数说明

## **1. 配置文件结构概述**
该配置文件定义了一个模型的工作流配置，包含模型列表、具体模型的参数以及工作流节点的详细配置。以下是配置文件的主要结构：
- **`model_list`**：模型名称列表。
- **`H_DET_BIRD_AH`**：具体模型的配置。
  - **`use`**：是否启用该模型。
  - **`device`**：运行设备编号。
  - **`workflow`**：工作流配置，包含多个节点的详细参数。

---

## **2. 参数说明**

### **2.1 全局参数**
#### **`model_list`**
- **类型**：数组
- **描述**：列出所有可用的模型名称。
- **示例**：
  ```json
  "model_list": ["H_DET_BIRD_AH"]
  ```

---

### **2.2 模型参数**
#### **`H_DET_BIRD_AH`**
- **类型**：对象
- **描述**：具体模型 `H_DET_BIRD_AH` 的配置。

##### **`use`**
- **类型**：整数
- **描述**：是否启用该模型。`0` 表示禁用，`1` 表示启用。
- **示例**：
  ```json
  "use": 0
  ```

##### **`device`**
- **类型**：整数
- **描述**：指定运行设备编号（如 GPU 编号）。
- **示例**：
  ```json
  "device": 1
  ```

##### **`workflow`**
- **类型**：对象
- **描述**：定义工作流的节点及其参数。

---

### **2.3 工作流参数**
#### **`node_set`**
- **类型**：数组
- **描述**：定义工作流中包含的节点名称列表。
- **示例**：
  ```json
  "node_set": ["node1", "node2", "node3"]
  ```

---

#### **`node1`**
- **类型**：对象
- **描述**：第一个工作流节点的配置。

##### **`model_type`**
- **类型**：字符串
- **描述**：模型类型。此处为 `"detection"`，表示目标检测模型。
- **示例**：
  ```json
  "model_type": "detection"
  ```

##### **`height` 和 **`width`**
- **类型**：整数
- **描述**：输入图像的高度和宽度（单位：像素）。
- **示例**：
  ```json
  "height": 768,
  "width": 768
  ```

##### **`model_file`**
- **类型**：字符串
- **描述**：模型文件路径。
- **示例**：
  ```json
  "model_file": "/data/01_Project/Saida-runtime-ubuntu22.04-sanhe/build/model_zoo/H_DET_BIRD_AH.part1.engine"
  ```

##### **`out_num`**
- **类型**：整数
- **描述**：输出类别数量。
- **示例**：
  ```json
  "out_num": 34
  ```

##### **`output_id`**
- **类型**：数组
- **描述**：输出 ID 列表，用于过滤特定类别的输出。空数组表示不过滤。
- **示例**：
  ```json
  "output_id": []
  ```

##### **`save_img_max_num`**
- **类型**：整数
- **描述**：最多保存的图片数量。
- **示例**：
  ```json
  "save_img_max_num": 500
  ```

##### **`preprocess`**
- **类型**：字符串
- **描述**：预处理方法。例如 `"rgb_letterbox"` 表示将图像缩放到指定尺寸并填充。
- **示例**：
  ```json
  "preprocess": "rgb_letterbox"
  ```

---

#### **`node2`**
- **类型**：对象
- **描述**：第二个工作流节点的配置。

##### **`model_type`**
- **类型**：字符串
- **描述**：模型类型。此处为 `"operation"`，表示操作节点。
- **示例**：
  ```json
  "model_type": "operation"
  ```

##### **`model_file`**
- **类型**：字符串
- **描述**：模型文件或操作类型。此处为 `"image_crop"`，表示裁剪操作。
- **示例**：
  ```json
  "model_file": "image_crop"
  ```

##### **`max_len`**
- **类型**：整数
- **描述**：裁剪操作的最大边长限制。
- **示例**：
  ```json
  "max_len": 32
  ```

##### **`output_id`**
- **类型**：数组
- **描述**：输出 ID 列表，用于过滤特定类别的输出。空数组表示不过滤。
- **示例**：
  ```json
  "output_id": []
  ```

---

#### **`node3`**
- **类型**：对象
- **描述**：第三个工作流节点的配置。

##### **`model_type`**
- **类型**：字符串
- **描述**：模型类型。此处为 `"classification"`，表示分类模型。
- **示例**：
  ```json
  "model_type": "classification"
  ```

##### **`model_file`**
- **类型**：字符串
- **描述**：模型文件路径。
- **示例**：
  ```json
  "model_file": "/data/01_Project/Saida-runtime-ubuntu22.04-sanhe/last_epoch_weights_V20250228065033.engine"
  ```

##### **`classifier_action`**
- **类型**：字符串
- **描述**：分类器的操作策略。
  - `"labal_replace"`：标签替换，用分类器的结果替换检测器的标签。
  - `"box_delete"`：删除负样本框，分类器判定为负样本时删除检测结果。
- **示例**：
  ```json
  "classifier_action": "box_delete"
  ```

##### **`classifier_action_配置说明`**
- **类型**：字符串
- **描述**：对 `classifier_action` 参数的补充说明。
- **示例**：
  ```json
  "classifier_action_配置说明": "labal_replace: 标签替换,将用分类器判定的label结果替换检测器label; box_delete: 删除负样本box,分类器对检测器结果判定,如果为负样本,就将这个检测结果删除"
  ```

##### **`output_id`**
- **类型**：数组
- **描述**：输出 ID 列表，用于过滤特定类别的输出。空数组表示不过滤。
- **示例**：
  ```json
  "output_id": []
  ```

##### **`height` 和 **`width`**
- **类型**：整数
- **描述**：输入图像的高度和宽度（单位：像素）。
- **示例**：
  ```json
  "height": 64,
  "width": 64
  ```

##### **`out_num`**
- **类型**：整数
- **描述**：输出类别数量。
- **示例**：
  ```json
  "out_num": 2
  ```

##### **`preprocess`**
- **类型**：字符串
- **描述**：预处理方法。例如 `"rgb_letterbox"` 表示将图像缩放到指定尺寸并填充。
- **示例**：
  ```json
  "preprocess": "rgb_letterbox"
  ```

---